1.
Поясніть різницю між класом і об'єктом
клас є шаблоном який вказує яки властивості мають бути у всіх обʼєктах яки можуть бути створенні в цьому класі в свою чергу обʼєкт це представник класу зі своїм місцем в памʼяті та властивостями класу
2.
Які чотири основні принципи ООП?
абстракція, інкапсуляція, успадкування, поліморфізм
3.
Що таке self у Python?
параметр який вказує що передані аргументи належать тількі цьому обʼєкту класу
4.
Чим відрізняються методи класу (@classmethod) і статичні методи (@staticmethod)?
(@staticmethod) забов'язує об'єкти класу використовувати функції / методи. (@classmethod) змінює методи всього класу
5.
Чи працює slice (нарізання) в list()?

Так

Ні
6.
Чи працює slice (нарізання) в str()?

Так

Ні
7.
Який ви знаете не впорядкований тип даних (not ordered)?

dict

str

set

list

tuple
8.
По яким вбудованим типам даних можна ітеруватись?
list, tuple, string, dictionary, set
9.
Поясніть різницю між ітератором і ітерабельним об'єктом
ітерабельний обʼєкт це обʼєкт який можно розібрати на складові, ітератор це об'єкт, який дозволяє отримувати елементи колекції один за одним
10.
Який магічний метод відповідає за створення класу?

__init__

__new__

__str__

__repr__
11.
Що таке *args та **kwargs у функціях?
*args передає будь які аргументи в функцію, **kwargs передає аргументи словником
12.
Що таке yield?
yield запом'ятовує останнє значеня роботи функції (створює обʼєкт) та дозволяє продовжити функцію з цього місця